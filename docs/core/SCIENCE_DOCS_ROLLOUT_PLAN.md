# Science Docs Rollout Plan

## Current rule

For now, work should stay in `docs/`.

Do not move into the top-level `S1...S6` directories until the structural layer is sufficiently filled.

This is especially important because:

- the repo still needs a stable documentation-first operating layer
- top-level science folders should receive only stable outputs, not every draft

## Current phase

### Phase 1: architecture and lane maps

Status:

- `S1-S6` sphere architecture: ready
- `S1-S6` lane maps: ready

### Phase 2: execution status and starter guides

Status:

- `S1`: ready
- `S2`: active and partially filled
- `S3`: status layer ready
- `S4`: active R1/R2 execution layer ready
- `S5`: active R1/R2 execution layer ready
- `S6`: active K-EcoLOGIC execution layer ready for S6-A

### Phase 3: actual research execution

This starts only after enough `docs/` structure exists.

Execution will mean:

- source registry
- evidence schema
- report skeleton
- then actual analysis and outputs

## Current priority order

1. finish structural `docs/` for `S2-S6`
2. stop expanding structure when each sphere has a usable execution layer
3. then move one by one into real research

## First execution order after docs phase

1. `S2`
2. `S3`
3. `S5`
4. `S4`
5. `S6-A`
6. `S6-D`
7. `S6-C`

## Practical rule

When a sphere is structurally ready, the next move is:

1. pass the structure approval gate
2. source registry
3. evidence schema
4. report skeleton
5. actual notes, figures, and outputs
6. only then mirror into the real `S` section

## Why this is the right sequence

This avoids two bad outcomes:

- too much abstract planning forever
- too-early file clutter in the actual science folders
