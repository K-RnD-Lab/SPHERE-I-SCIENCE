name: sortsmart_ukraine
version: 1.0.0
config-version: 2

profile: sortsmart_ukraine

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  sortsmart_ukraine:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  sortsmart_ukraine:
    +quote_columns: false
